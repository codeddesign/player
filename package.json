{
    "scripts": {
        "roll": "rollup -c rollup.config.js --environment build:production",
        "rollt": "rollup -c rollup.config.js --environment build:test",
        "rolld": "rollup -c rollup.config.js --environment build:dev -w"
    },
    "devDependencies": {
        "rollup": "^0.34.13",
        "rollup-plugin-buble": "^0.13.0",
        "rollup-plugin-replace": "^1.1.1",
        "rollup-plugin-uglify": "^1.0.1",
        "rollup-watch": "^2.5.0"
    }
}
